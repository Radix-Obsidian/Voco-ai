{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for Voco MCP Gateway — Zero-Trust search + execution scope",
  "windows": ["main", "orb"],
  "permissions": [
    "core:default",
    "fs:default",
    "global-shortcut:allow-register",
    "global-shortcut:allow-unregister",
    "global-shortcut:allow-is-registered",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "binaries/rg",
          "sidecar": true,
          "args": [
            { "validator": "^--[a-zA-Z][a-zA-Z0-9-]*$" },
            { "validator": "^--[a-zA-Z][a-zA-Z0-9-]*$" },
            { "validator": "^--[a-zA-Z][a-zA-Z0-9-]*$" },
            { "validator": "^--[a-zA-Z][a-zA-Z0-9-]*$" },
            { "validator": "\\S+" },
            { "validator": "^([A-Za-z]:\\\\|/).*$" }
          ]
        },
        {
          "name": "cmd",
          "cmd": "cmd",
          "args": true
        },
        {
          "name": "sh",
          "cmd": "sh",
          "args": true
        }
      ]
    }
  ]
}
